pipeline {
    agent any
    stages {
        stage('Checkout') {
          steps {
              git branch: 'main', url:
'https://github.com/username/repo.git'
                }
                          }
        stage('Build') {
          steps {
                  // Add build steps here, for example:
                sh './build.sh'
                }
                       }
       stage('Test') {
         steps {
                 // Add test steps here
sh './run_tests.sh'
               }
                    }
           }
         }
